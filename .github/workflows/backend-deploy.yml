name: Deploy Backend

on:
  push:
    branches:
      - backend  # Triggers only when pushing to the backend branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"